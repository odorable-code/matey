spring.application.name=matey

mybatis.configuration.map-underscore-to-camel-case=true

# ===============================
# Swagger (API ë¬¸ì ìëí) ì¤ì 
# ===============================

# Swagger UI ì ì ê²½ë¡:  http://localhost:8080/swagger-ui.html ë¡ ì ì ê°ë¥
springdoc.swagger-ui.path=/swagger-ui.html

# Swagger UIìì API ì ë ¬ ê¸°ì¤ (HTTP Method ê¸°ì¤ ì ë ¬)
# ex) GET â POST â PUT â DELETE
springdoc.swagger-ui.operations-sorter=method

# íê·¸ ì´ë¦ì ìíë²³ ìì¼ë¡ ì ë ¬
springdoc.swagger-ui.tags-sorter=alpha

# Swaggerê° ì¤ìºí  Controller í¨í¤ì§ ì§ì 
# ì´ ê²½ë¡ ìì ìë @RestController ë¤ë§ ë¬¸ìíë¨
springdoc.packages-to-scan=kr.hi.matey.controller

# ===============================
# ë°ì´í°ë² ì´ì¤(MySQL) ì°ê²° ì¤ì 
# ===============================

spring.datasource.username=root
spring.datasource.password=root
# MySQL JDBC ëë¼ì´ë² í´ëì¤
# MySQL 8 ì´ìììë ë°ëì com.mysql.cj.jdbc.Driver ì¬ì©
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.jdbc-url=jdbc:mysql://localhost:3306/matey?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&allowMultiQueries=true

# true: 기동 시 테스트 계정 비밀번호를 동기화(로컬 편의). 운영은 false.
matey.sync-test-user-passwords=false

# true: 기동 시 BOT.card_stats_json 이 비어 있으면 dog/bear/cat 기본 JSON 채움(메인 막대). 운영 수동만 쓰면 false.
matey.seed-bot-card-stats=true

# JWT ì¤ì 
# 1. Secret Key: ë³´ìì ìí´ ë¬´ììì ê¸´ ë¬¸ìì´ì ìë ¥
# 운영에서는 반드시 JWT_SECRET 환경 변수로 덮어쓰기
jwt.secret=${JWT_SECRET:vms70m19024m30m503m1023m102m305m1023m105m1023m102m305m1023m}

# 2. Access Token ë§ë£ ìê°: 1,800ì´ (30ë¶)
jwt.token-validity-in-seconds=1800

# (ì í) Refresh Tokenì ì¬ì©íë¤ë©´ ì¶ê° ì¤ì : 604,800ì´ (7ì¼)
jwt.refresh-token-validity-in-seconds=604800

app.frontend-url=${FRONTEND_URL:http://localhost:3000}
# 로컬 개발: 포트·호스트 혼용 시 CORS (프런트 Failed to fetch) 방지 — 패턴은 Spring 5.3+ 에서 허용
app.cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:*,http://127.0.0.1:*,http://localhost:3000,http://127.0.0.1:3000,http://3.38.49.151:3000,http://3.38.49.151}

# ì¹´ì¹´ì¤/ë¤ì´ë² ê´ë ¨
oauth.providers.naver.client-id=${NAVER_CLIENT_ID}
oauth.providers.naver.client-secret=${NAVER_CLIENT_SECRET}
# redirect-uri : ì¶í ìì í  ê²
oauth.providers.naver.redirect-uri=${NAVER_REDIRECT_URI:http://localhost:8080/oauth2/callback/naver}
oauth.providers.naver.authorize-uri=https://nid.naver.com/oauth2.0/authorize
oauth.providers.naver.token-uri=https://nid.naver.com/oauth2.0/token
oauth.providers.naver.user-info-uri=https://openapi.naver.com/v1/nid/me

oauth.providers.kakao.client-id=${KAKAO_CLIENT_ID}
oauth.providers.kakao.client-secret=${KAKAO_CLIENT_SECRET:}
# redirect-uri : ì¶í ìì í  ê²
oauth.providers.kakao.redirect-uri=${KAKAO_REDIRECT_URI:http://localhost:8080/oauth2/callback/kakao}
oauth.providers.kakao.authorize-uri=https://kauth.kakao.com/oauth/authorize
oauth.providers.kakao.token-uri=https://kauth.kakao.com/oauth/token
oauth.providers.kakao.user-info-uri=https://kapi.kakao.com/v2/user/me

social.success-path=/social/success

# Mail — 로컬/운영 모두 MAIL_USERNAME, MAIL_PASSWORD 환경 변수 설정 (저장소에 비밀번호 금지)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
