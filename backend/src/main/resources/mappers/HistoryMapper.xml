<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.hi.matey.dao.HistoryMapper">

    <select id="selectHistoryList" resultType="kr.hi.matey.dto.HistoryDTO">
        SELECT
        id,
        title,
        summary,
        mood,
        date,
        created_at as createdAt,
        counselor,
        duration,
        tags as tagsRaw
        FROM counsel_history
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="selectHistoryById" resultType="kr.hi.matey.dto.HistoryDTO">
        SELECT
        id,
        title,
        summary,
        mood,
        date,
        created_at as createdAt,
        counselor,
        duration,
        tags as tagsRaw
        FROM counsel_history
        WHERE id = #{id}
    </select>

</mapper>