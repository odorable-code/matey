<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.hi.matey.dao.ReportDAO">

    <insert id="insertReport"
            parameterType="kr.hi.matey.dto.ReportDTO"
            useGeneratedKeys="true"
            keyProperty="supportId">
        INSERT INTO support (
            type,
            target_type,
            target_id,
            reason,
            content,
            status,
            created_at,
            user_id
        )
        VALUES (
            #{type},
            #{targetType},
            #{targetId},
            #{reason},
            #{content},
            'PENDING',
            NOW(),
            #{userId}
        )
    </insert>

    <insert id="insertReportImage">
        INSERT INTO support_image (
            image_url,
            created_at,
            support_id
        )
        VALUES (
            #{imageUrl},
            NOW(),
            #{supportId}
        )
    </insert>

    <select id="existsReason" resultType="int">
        SELECT COUNT(*)
        FROM support_reason
        WHERE reason_id = #{reason}
        AND is_active = 1
    </select>

    <select id="selectReasonList" resultType="kr.hi.matey.dto.ReportReasonDTO">
        SELECT
            reason_id AS reasonId,
            reason_name AS reasonName
        FROM support_reason
        WHERE is_active = 1
        ORDER BY reason_id
    </select>

</mapper>