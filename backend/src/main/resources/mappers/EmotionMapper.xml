<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.hi.matey.dao.EmotionMapper">

    <select id="selectEmotionHistory" resultType="kr.hi.matey.dto.EmotionDTO">
        SELECT
        id,
        user_id as userId,
        emotion,
        score,
        note,
        created_at as createdAt
        FROM emotion_logs
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <insert id="insertEmotionLog" parameterType="kr.hi.matey.dto.EmotionDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO emotion_logs (
        user_id,
        emotion,
        score,
        note,
        created_at
        ) VALUES (
        #{userId},
        #{emotion},
        #{score},
        #{note},
        NOW()
        )
    </insert>

</mapper>