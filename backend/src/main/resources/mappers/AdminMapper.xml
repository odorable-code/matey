<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hi.matey.dao.AdminDAO">

    <!-- ========================================== -->
    <!-- 실시간 운영 통계 및 지표 -->
    <!-- ========================================== -->
    <select id="selectDashboardOverview" resultType="map">
        SELECT
            (SELECT COUNT(*) FROM `USER`) as totalUsers,
            (SELECT COUNT(*) FROM `USER` WHERE status = 'ACTIVE') as activeUsers,
            (SELECT COUNT(*) FROM `USER` WHERE created_at >= CURDATE()) as newUsersToday,
            (SELECT COUNT(*) FROM SUPPORT WHERE status = 'PENDING') as pendingFeedbacks,
            (SELECT COUNT(*) FROM USER_ROLE ur JOIN ROLE r ON ur.role_id = r.role_id
             WHERE r.role_code IN ('ADMIN', 'SUPER_ADMIN')) as adminCount,
            (SELECT COUNT(*) FROM MESSAGE WHERE emotion_id IS NOT NULL) as totalReports,
            (SELECT COUNT(DISTINCT chat_room_id) FROM CHAT_ROOM
             WHERE updated_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)) as activeConversations
    </select>

    <select id="selectLiveMetrics" resultType="map">
        SELECT
            DATE_FORMAT(created_at, '%H:00') as label,
            COUNT(*) as value
        FROM MESSAGE
        WHERE created_at >= DATE_SUB(NOW(), INTERVAL 8 HOUR)
        GROUP BY label
        ORDER BY label ASC
    </select>

    <select id="selectEmotionDistribution" resultType="map">
        SELECT
            ec.emotion_name as label,
            COUNT(m.message_id) as value
        FROM MESSAGE m
        INNER JOIN EMOTION_CATEGORY ec ON m.emotion_id = ec.emotion_id
        GROUP BY ec.emotion_id, ec.emotion_name
        ORDER BY value DESC
    </select>

    <select id="selectConcernDistribution" resultType="map">
        SELECT
            c.name as label,
            COUNT(p.post_id) as value
        FROM POST p
        JOIN CATEGORY c ON p.category_id = c.category_id
        GROUP BY c.category_id, c.name
        ORDER BY value DESC
    </select>

    <!-- ========================================== -->
    <!-- 사용자 관리 (CRUD + 필터/검색) -->
    <!-- ========================================== -->
    <select id="selectUsers" resultType="kr.hi.matey.dto.UserDTO2">
        SELECT
            u.user_id as userId,
            u.email,
            u.nickname,
            u.status,
            u.login_type as loginType,
            u.last_login_at as lastLoginAt,
            u.created_at as createdAt,
            r.role_code as roleName,
            (SELECT COUNT(*)
             FROM CHAT_ROOM cr
             JOIN EXCLUSIVE e2 ON cr.exclusive_id = e2.exclusive_id
             WHERE e2.user_id = u.user_id) as conversationCount,
            0 as activityScore,
            COALESCE(
                (SELECT b.name FROM BOT b WHERE b.bot_id = u.assigned_bot_id),
                (SELECT b2.name
                 FROM EXCLUSIVE e
                 INNER JOIN BOT b2 ON e.bot_id = b2.bot_id
                 WHERE e.user_id = u.user_id
                 ORDER BY e.exclusive_id ASC
                 LIMIT 1)
            ) as assignedBotName,
            (SELECT COUNT(*)
             FROM SUPPORT s
             INNER JOIN SUPPORT_REASON sr ON s.support_reason_id = sr.support_reason_id
             WHERE s.user_id = u.user_id AND sr.reason_type = 'REPORT') as reportCount,
            (SELECT COUNT(*) FROM SUPPORT s WHERE s.user_id = u.user_id) as supportCount
        FROM `USER` u
        LEFT JOIN USER_ROLE ur ON u.user_id = ur.user_id
        LEFT JOIN ROLE r ON ur.role_id = r.role_id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (u.nickname LIKE CONCAT('%', #{keyword}, '%')
                OR u.email LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="role != null and role != 'ALL'">
                AND r.role_code = #{role}
            </if>
            <if test="status != null and status != 'ALL'">
                AND u.status = #{status}
            </if>
        </where>
        ORDER BY u.created_at DESC
    </select>

    <select id="selectUserById" resultType="kr.hi.matey.dto.UserDTO2">
        SELECT
            u.user_id as userId,
            u.email,
            u.nickname,
            u.status,
            u.login_type as loginType,
            u.last_login_at as lastLoginAt,
            u.created_at as createdAt,
            r.role_code as roleName,
            (SELECT COUNT(*)
             FROM CHAT_ROOM cr
             JOIN EXCLUSIVE e2 ON cr.exclusive_id = e2.exclusive_id
             WHERE e2.user_id = u.user_id) as conversationCount,
            0 as activityScore,
            COALESCE(
                (SELECT b.name FROM BOT b WHERE b.bot_id = u.assigned_bot_id),
                (SELECT b2.name
                 FROM EXCLUSIVE e
                 INNER JOIN BOT b2 ON e.bot_id = b2.bot_id
                 WHERE e.user_id = u.user_id
                 ORDER BY e.exclusive_id ASC
                 LIMIT 1)
            ) as assignedBotName,
            (SELECT COUNT(*)
             FROM SUPPORT s
             INNER JOIN SUPPORT_REASON sr ON s.support_reason_id = sr.support_reason_id
             WHERE s.user_id = u.user_id AND sr.reason_type = 'REPORT') as reportCount,
            (SELECT COUNT(*) FROM SUPPORT s WHERE s.user_id = u.user_id) as supportCount
        FROM `USER` u
        LEFT JOIN USER_ROLE ur ON u.user_id = ur.user_id
        LEFT JOIN ROLE r ON ur.role_id = r.role_id
        WHERE u.user_id = #{userId}
    </select>

    <update id="updateUser">
        UPDATE USER
        <set>
            <if test="data.nickname != null">nickname = #{data.nickname},</if>
            <if test="data.status != null">status = #{data.status},</if>
            updated_at = NOW()
        </set>
        WHERE user_id = #{userId}
    </update>

    <update id="updateUserRole">
        UPDATE USER_ROLE 
        SET role_id = (SELECT role_id FROM ROLE WHERE role_code = #{roleCode})
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser">
        -- DB 스키마의 ON DELETE CASCADE 설정으로 인해 USER 삭제 시 연관 데이터가 자동 삭제됩니다.
        DELETE FROM `USER` WHERE `user_id` = #{userId}
    </delete>

    <!-- ========================================== -->
    <!-- 일괄 작업 (Batch Operations) -->
    <!-- ========================================== -->
    <update id="bulkUpdateUserStatus">
        UPDATE USER SET status = #{status}, updated_at = NOW()
        WHERE user_id IN
        <foreach item="id" collection="userIds" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="bulkUpdateUserRole">
        UPDATE USER_ROLE 
        SET role_id = (SELECT role_id FROM ROLE WHERE role_code = #{roleCode})
        WHERE user_id IN
        <foreach item="id" collection="userIds" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- ========================================== -->
    <!-- 사용자 피드백 관리 -->
    <!-- ========================================== -->
    <select id="selectFeedbacks" resultType="kr.hi.matey.dto.FeedbackDTO">
        SELECT
            s.support_id as supportId,
            s.user_id as userId,
            u.nickname as userNickname,
            s.title,
            s.content,
            s.status,
            s.created_at as createdAt,
            sr.reason_name as reasonName,
            sr.reason_type as reasonType,
            sr.target_type as targetType,
            sa.content as answerContent,
            au.nickname as answerAdminNickname,
            sa.created_at as answerCreatedAt
        FROM SUPPORT s
        JOIN `USER` u ON s.user_id = u.user_id
        JOIN SUPPORT_REASON sr ON s.support_reason_id = sr.support_reason_id
        LEFT JOIN SUPPORT_ANSWER sa ON sa.answer_id = (
            SELECT sa2.answer_id
            FROM SUPPORT_ANSWER sa2
            WHERE sa2.support_id = s.support_id
            ORDER BY sa2.created_at DESC, sa2.answer_id DESC
            LIMIT 1
        )
        LEFT JOIN `USER` au ON sa.admin_user_id = au.user_id
        <where>
            <if test="status != null and status != 'ALL'">
                AND s.status = #{status}
            </if>
        </where>
        ORDER BY s.created_at DESC
    </select>

    <select id="selectFeedbackById" resultType="kr.hi.matey.dto.FeedbackDTO">
        SELECT
            s.support_id as supportId,
            s.user_id as userId,
            u.nickname as userNickname,
            s.title,
            s.content,
            s.status,
            s.created_at as createdAt,
            sr.reason_name as reasonName,
            sr.reason_type as reasonType,
            sr.target_type as targetType,
            sa.content as answerContent,
            au.nickname as answerAdminNickname,
            sa.created_at as answerCreatedAt
        FROM SUPPORT s
        JOIN `USER` u ON s.user_id = u.user_id
        JOIN SUPPORT_REASON sr ON s.support_reason_id = sr.support_reason_id
        LEFT JOIN SUPPORT_ANSWER sa ON sa.answer_id = (
            SELECT sa2.answer_id
            FROM SUPPORT_ANSWER sa2
            WHERE sa2.support_id = s.support_id
            ORDER BY sa2.created_at DESC, sa2.answer_id DESC
            LIMIT 1
        )
        LEFT JOIN `USER` au ON sa.admin_user_id = au.user_id
        WHERE s.support_id = #{supportId}
    </select>

    <update id="updateFeedbackStatus">
        UPDATE SUPPORT SET status = #{status}, updated_at = NOW() WHERE support_id = #{supportId}
    </update>

    <insert id="insertSupportAnswer">
        INSERT INTO SUPPORT_ANSWER (content, support_id, admin_user_id, created_at)
        VALUES (
            #{content},
            #{supportId},
            #{adminUserId},
            NOW()
        )
    </insert>

    <select id="countSupportAnswersBySupportId" resultType="int">
        SELECT COUNT(*) FROM SUPPORT_ANSWER WHERE support_id = #{supportId}
    </select>

    <select id="selectSupportUserId" resultType="long">
        SELECT user_id FROM SUPPORT WHERE support_id = #{supportId}
    </select>

    <delete id="deleteFeedback">
        DELETE FROM SUPPORT WHERE support_id = #{supportId}
    </delete>

    <!-- 상담봇: 누적 좋/싫 + 전월 추천 이벤트 건별 집계 -->
    <select id="selectBotsWithPrevMonthBreakdown" resultType="kr.hi.matey.dto.AdminBotStatsRow">
        SELECT
            b.bot_id AS botId,
            b.name AS name,
            b.description AS description,
            b.avatar_image AS avatarImage,
            b.like_count AS likeCount,
            b.dislike_count AS dislikeCount,
            COALESCE(s.net, 0) AS prevMonthNet,
            COALESCE(s.reco, 0) AS prevMonthRecommendCount,
            COALESCE(s.disc, 0) AS prevMonthDislikeCount
        FROM BOT b
        LEFT JOIN (
            SELECT
                e.bot_id AS bot_id,
                SUM(e.delta) AS net,
                SUM(CASE WHEN e.delta = 1 THEN 1 ELSE 0 END) AS reco,
                SUM(CASE WHEN e.delta = -1 THEN 1 ELSE 0 END) AS disc
            FROM BOT_RECOMMEND_EVENT e
            WHERE e.created_at &gt;= STR_TO_DATE(CONCAT(#{year}, '-', LPAD(#{month}, 2, '0'), '-01 00:00:00'), '%Y-%m-%d %H:%i:%s')
              AND e.created_at &lt; DATE_ADD(
                  STR_TO_DATE(CONCAT(#{year}, '-', LPAD(#{month}, 2, '0'), '-01 00:00:00'), '%Y-%m-%d %H:%i:%s'),
                  INTERVAL 1 MONTH
              )
            GROUP BY e.bot_id
        ) s ON s.bot_id = b.bot_id
        ORDER BY b.bot_id ASC
    </select>

    <select id="countRecommendEventsInMonth" resultType="long">
        SELECT COUNT(*)
        FROM BOT_RECOMMEND_EVENT e
        WHERE e.created_at &gt;= STR_TO_DATE(CONCAT(#{year}, '-', LPAD(#{month}, 2, '0'), '-01 00:00:00'), '%Y-%m-%d %H:%i:%s')
          AND e.created_at &lt; DATE_ADD(
              STR_TO_DATE(CONCAT(#{year}, '-', LPAD(#{month}, 2, '0'), '-01 00:00:00'), '%Y-%m-%d %H:%i:%s'),
              INTERVAL 1 MONTH
          )
    </select>

</mapper>
