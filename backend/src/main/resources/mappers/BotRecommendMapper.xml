<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.hi.matey.dao.BotRecommendDAO">

    <select id="selectReaction" resultType="java.lang.Integer">
        SELECT reaction
        FROM USER_BOT_RECOMMEND
        WHERE user_id = #{userId}
          AND bot_id = #{botId}
    </select>

    <select id="selectReactionsByUser" resultType="kr.hi.matey.dto.UserBotReactionRow">
        SELECT bot_id AS botId, reaction AS reaction
        FROM USER_BOT_RECOMMEND
        WHERE user_id = #{userId}
    </select>

    <insert id="insertWithReaction">
        INSERT INTO USER_BOT_RECOMMEND (user_id, bot_id, reaction, created_at)
        VALUES (#{userId}, #{botId}, #{reaction}, NOW())
    </insert>

    <update id="incrementBotLike">
        UPDATE BOT
        SET like_count = like_count + 1
        WHERE bot_id = #{botId}
    </update>

    <update id="decrementBotLike">
        UPDATE BOT
        SET like_count = GREATEST(0, like_count - 1)
        WHERE bot_id = #{botId}
    </update>

    <update id="incrementBotDislike">
        UPDATE BOT
        SET dislike_count = dislike_count + 1
        WHERE bot_id = #{botId}
    </update>

    <update id="decrementBotDislike">
        UPDATE BOT
        SET dislike_count = GREATEST(0, dislike_count - 1)
        WHERE bot_id = #{botId}
    </update>

    <select id="selectBotLikeCount" resultType="java.lang.Integer">
        SELECT like_count
        FROM BOT
        WHERE bot_id = #{botId}
    </select>

    <select id="selectBotDislikeCount" resultType="java.lang.Integer">
        SELECT dislike_count
        FROM BOT
        WHERE bot_id = #{botId}
    </select>

    <insert id="insertRecommendEvent">
        INSERT INTO BOT_RECOMMEND_EVENT (user_id, bot_id, delta, created_at)
        VALUES (#{userId}, #{botId}, #{delta}, NOW())
    </insert>

    <update id="updateReaction">
        UPDATE USER_BOT_RECOMMEND
        SET reaction = #{reaction}
        WHERE user_id = #{userId}
          AND bot_id = #{botId}
    </update>

    <delete id="deleteReaction">
        DELETE FROM USER_BOT_RECOMMEND
        WHERE user_id = #{userId}
          AND bot_id = #{botId}
    </delete>

</mapper>
