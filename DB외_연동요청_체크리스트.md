# DB 외 연동요청 체크리스트 (설명용)

이 문서는 **DB 담당자가 타 직군(백엔드/프론트/기획/운영)에 요청할 항목**을 정리한 설명용 문서다.  
대상: `상담_메시지_DB_저장.md`의 결정사항을 실제 서비스에 반영해야 하는 담당자.

---

## 1) 백엔드(서버) 요청사항

## 1.1 인증/보안

- 비밀번호 재설정 성공 시, 해당 사용자의 자동로그인/세션/토큰을 즉시 무효화하는 로직 필요.
- 무효화 범위(현재 기기만 vs 전체 기기)는 정책 확정 필요.
- 관리자 권한 변경(ADMIN 승격/해제)은 슈퍼관리자만 실행 가능하도록 권한 체크 필요.

## 1.2 채팅 메시지 처리

- 메시지 저장은 `MESSAGE` 1행 단위 INSERT로 통일.
- 채팅방 메타(`last_message_at`, 미리보기 등) 갱신 로직 필요.
- 채팅방 진입 시 커서 기반 페이징 API 필요 (`before/after message_id`).
- 감정 분석은 비동기 처리(큐/워크커) 여부 결정 필요.

## 1.3 친밀도/먹이/모션 해금

- 먹이 지급 시 트랜잭션 내에서:
  - `FEED_TRANSACTION` 기록
  - 친밀도 점수 갱신
  - 해금 가능한 모션 계산
- 친밀도 레벨 계산은 기준 테이블 버전 기반으로 처리 필요.

## 1.4 관리자 기능

- ADMIN 가능 기능:
  - FAQ 작성/수정
  - 문의/신고 답변
  - 공지 작성
  - 사용자 기간 정지
- SUPER_ADMIN 전용 기능:
  - 관리자 임명/해제
  - 권한 정책 변경
- 제재(기간 정지) 배치/검증 로직 필요 (만료 시 자동 해제 등).

## 1.5 문의/신고 운영

- 사용자 문의/신고 등록 -> 관리자 답변 -> 상태 완료 플로우 API 필요.
- 문의 분류(`INQUIRY`, `REPORT` 등)를 입력 검증으로 강제 필요.

---

## 2) 프론트(웹/데스크톱) 요청사항

## 2.1 채팅 화면 구성

- 채팅방 화면을 2영역으로 구성:
  - 봇 영역(아바타/모션)
  - 채팅 영역(말풍선/입력/시간)
- 모바일 앱 전용 UX(전화번호 친구, 하단 탭 제스처 중심 구조)는 제외.

## 2.2 채팅 UX

- 무한 스크롤 또는 이전 메시지 불러오기(커서 페이징) UI 필요.
- 사용자가 방 삭제 시:
  - 즉시 사라지는 것이 소프트 삭제임을 안내
  - 연말(또는 정책 일자) 완전 삭제 문구 표시

## 2.3 관리자 페이지

- FAQ/공지/문의답변/정지관리 화면 분리 필요.
- 관리자 메뉴 노출은 권한별로 분기 필요(ADMIN vs SUPER_ADMIN).

---

## 3) 기획/정책 확정 필요사항

- 봇 좋아요 저장 방식 택1:
  - `BOT_REACTION` 단일 사용
  - `BOT_LIKE` 전용 테이블 사용
- 사료 구조:
  - 단일 사료 유지
  - 다중 상품(`FEED_PRODUCT`) 확장
- 분석 주기:
  - 실시간 분석
  - 일 배치 분석
- 데이터 보존:
  - 원문 메시지 보관 기간
  - 분석 결과 보관 기간
- `updated_at` 제거 기준:
  - 감사/배치/운영 쿼리에서 실제 사용하는지 확정 필요.

---

## 4) 운영/CS 요청사항

- FAQ는 사용자 작성이 아니라 운영자가 등록하는 정책으로 확정.
- FAQ 등록 기준:
  - 문의/신고에서 자주 반복되는 질문
- 사용자 기간 정지 시:
  - 정지 사유 템플릿
  - 정지 종료 자동 복구 정책
- 고위험 감정 탐지 시 운영 대응 프로세스(알림/검토/에스컬레이션) 수립 필요.

---

## 5) QA(테스트) 체크포인트

- 비밀번호 재설정 후 기존 자동로그인 토큰이 실제로 차단되는지.
- 메시지 대량 데이터(장기 상담)에서도 조회 성능이 유지되는지.
- 먹이 지급 -> 친밀도 상승 -> 모션 해금이 순서대로 반영되는지.
- 관리자 권한 오남용 차단:
  - ADMIN이 관리자 임명을 못 하는지
  - SUPER_ADMIN만 임명 가능한지
- 문의/신고 상태 전이(`PENDING -> DONE`)가 정상 동작하는지.

---

## 6) 전달용 한 줄 요약

- DB 구조는 정리되어 있으니, 이제 **서버 권한 로직/채팅 API/관리자 UI/운영정책**을 각 담당이 맞춰 구현하면 된다.

---

## 7) 추가로 논의하면 좋은 항목 (선택·비 DB)

아래는 이전에 논의한 내용 중 **체크리스트에 없었거나 한 줄만 더 전달하면 좋은 것**이다.

### 백엔드·인프라

- **실시간 채팅**: 신규 메시지 수신을 위해 WebSocket/SSE 여부, 재연결 정책.
- **읽음 처리**: `last_read_message_id` 사용 시, 방 진입·스크롤 시 읽음 갱신 API.
- **채팅방 아카이브(3시간 미작동)**: 스케줄러/배치로 `CHAT_ROOM.status` 갱신 여부.
- **연말(정책 일자) 물리 삭제 배치**: 소프트 삭제된 방·메시지 정리 잡.
- **인기 통계 배치**: `BOT_POPULARITY_STAT` 집계 주기·재실행 정책.
- **첨부파일**: 메시지 이미지·파일 시 객체 스토리지 업로드 URL·만료 정책(비 DB).
- **남용 방지**: 채팅·커뮤니티 API 레이트 리밋·스팸 필터 담당 범위.

### 프론트·UX

- **봇 모션 에셋**: CDN 경로·버전 관리(배포 시 깨짐 방지).
- **브라우저 알림**: 웹에서 선택 알림 시 권한 요청 UX(모바일 앱 푸시와 구분).

### 기획·법무·운영

- **계정 탈퇴·개인정보**: 삭제 범위(메시지 포함 여부), 유예 기간 — DB 스키마와 별도 합의.
- **메시지 암호화**: 저장 시 암호화 여부는 **애플리케이션/키 관리** 주제.
- **고위험 감정 후속 조치**: 알림 수신자, SLA, 에스컬레이션은 운영 프로세스 문서화.

### QA 보강

- 소프트 삭제 후 목록·검색에서 제외되는지, 배치 삭제 후 완전히 조회 불가인지.
- 슈퍼관리자 **최소 1명** 부재 상태 방지(임명 마지막 한 명 삭제 방지 등).
