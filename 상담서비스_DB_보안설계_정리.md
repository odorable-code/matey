# 상담 서비스 데이터/보안 설계 정리 (개념 중심)

## 1. 목표 정리

이 서비스는 상담 채팅을 중심으로 다음 기능을 함께 운영한다.

- 사용자-봇 상담 채팅
- 상담 종료 후 상담과 봇에 대한 평가
- 사용자 메시지 감정 분석 기반 상담 이어가기
- 상담 요약 기반의 연속성 있는 대화 경험(원문 삭제 후에도 유지)
- 주/월/년 감정 변화 및 고민 주제 대시보드
- 연간 요약 이벤트와 "모닥불 태우기(사용자 직접 실행)" 경험
- 과거 상담 열람(단, 보관 기간 이후 삭제)
- 상담 기반 봇 쪽지 제공

핵심은 데이터를 한곳에 몰아두지 않고, 목적별로 나누어 관리하는 것이다.

---

## 2. 전체 설계 원칙

### 2.1 데이터 분리 원칙
- 상담 원문, 분석 결과, 요약 데이터, 통계 데이터를 분리한다.
- 원문은 민감정보로 보고 가장 강한 보호 정책을 적용한다.
- 봇의 장기 문맥은 원문이 아니라 요약 데이터 중심으로 유지한다.
- 대시보드는 가능한 한 원문이 아닌 분석/통계 데이터로 구성한다.

### 2.2 최소 수집 원칙
- 서비스 운영에 필요한 정보만 저장한다.
- 불필요한 개인정보와 장기 보관 데이터는 줄인다.

### 2.3 기간 기반 생명주기 원칙
- 생성, 열람, 요약, 삭제의 흐름을 기간 정책으로 명확히 관리한다.
- 월간/연간 이벤트 정책을 데이터 삭제 기준과 연결한다.

---

## 3. 데이터 영역(테이블 묶음) 제안

상담내용 관련 구조는 `상담_메시지_DB_저장` 파일 기준으로 정리하고,  
그 외 영역은 현재 문서 기준(개념 중심)으로 유지한다.

### 3.1 채팅 영역
- 사용자
- 채팅방
- 메시지(사용자/봇/시스템)
- 봇 메시지 평가(좋아요/싫어요)
- 상담 평가 테이블: 메시지T, 점수T, 사유T

### 3.2 분석 영역
- 메시지별 감정 분석 결과
- 감정 카테고리 정의
- 감정별 점수 저장(0~1 범위)
- 분석 버전 이력(모델 변경 시 비교 가능)

### 3.3 이벤트/콘텐츠 영역
- 상담 요약 테이블: 사용자T, 봇T, 요약내용T
- 상담 요약 저장 규칙: 사용자id-봇id 1:1 조합 단위로 관리
- 사용자id가 같고 봇id가 다를 때 저장 데이터가 없으면 새 데이터 추가
- 봇id가 같고 사용자id가 다를 때 저장 데이터가 없으면 새 데이터 추가
- 연간 요약 이벤트
- 모닥불 태우기 실행 기록(사용자 직접 실행)
- 봇 쪽지(격려/회고/체크인)

### 3.4 정책/운영 영역
- 보관 기간 정책
- 삭제 실행 이력
- 접근/열람 감사 기록

### 3.5 상담평가 영역 (세션 단위)
- 상담 종료 후 사용자 평가 데이터
- 점수 테이블(정량)과 사유 테이블(정성) 분리 저장
- 메시지 단위 반응과 별도로 관리하여 상담 전체 만족도를 추적

---

## 4. 상담내용 DB 구조 (상담_메시지_DB_저장 기준)

### 4.1 감정 카테고리
- 감정 종류를 코드화하여 관리한다.
- 하드코딩을 피하고, 활성/비활성 상태를 두어 운영 중 변경 가능하게 한다.
- 예: 분노, 슬픔, 불안, 기쁨, 중립

### 4.2 메시지 분석
- 사용자 메시지 1건에 대해 분석 결과 1건을 저장한다.
- 대표 감정(지배 감정), 위험도, 고위험 여부, 모델명/버전을 함께 기록한다.
- 같은 메시지의 중복 분석 저장을 방지하는 정책을 둔다.

### 4.3 감정별 점수
- 한 분석 결과 안에 여러 감정 점수를 저장한다.
- 점수는 퍼센트 대신 0~1 소수로 저장하고, 화면에서 %로 변환한다.
- 예: 0.6500 -> 65.00%

### 4.4 관계 구조
- `메시지` 1 - 1 `메시지 분석`
- `메시지 분석` 1 - N `감정별 점수`
- `감정 카테고리` 1 - N `감정별 점수`

정리하면:

- 감정카테고리 -> 감정별 점수 <- 메시지분석 <- 메시지

이 구조로 가면 감정 확장, 모델 교체, 통계 집계가 모두 유연해진다.

---

## 5. JSON 통합 저장 vs 분리 저장

전체 상담을 JSON 한 덩어리로만 저장하면 다음과 같은 어려움이 생긴다.

- 일부 기간/일부 메시지 삭제가 복잡해짐
- 열람 권한을 세밀하게 나누기 어려움
- 통계/대시보드 연결이 비효율적임

따라서 기본 데이터는 분리 저장하고,  
이벤트용 요약/스냅샷이 필요할 때만 별도 형태로 관리하는 방식이 더 안전하다.

추가로, 메시지 데이터가 삭제된 이후에도 상담 연속성을 유지하려면  
요약 테이블을 별도로 두고 봇이 이 요약을 참조하도록 설계하는 것이 적합하다.

---

## 6. 월간/연간 "태우기" 정책 정리

요구사항에 맞춘 정책 해석:

- 연간: 연간 요약 이벤트와 연결된 모닥불 태우기만 운영
- 태우기 실행 주체: 사용자(직접 태우기 액션 수행)
- 태우기 대상: 정책에 따라 연간 범위 상담 원문 또는 이벤트 대상 데이터

운영에서 중요한 점:

- 삭제 기준(무엇을, 언제, 어떤 범위로)을 문서화
- 삭제 실행 내역을 남겨 추적 가능하게 관리
- 사용자에게 삭제 결과를 명확히 안내

---

## 7. 보안/비밀보장 원칙

### 7.1 저장 보호
- 상담 원문과 요약문은 보호된 형태로 저장
- 보관 기간이 지난 민감 데이터는 자동 삭제

### 7.2 접근 통제
- 사용자 본인 데이터만 조회 가능하게 제한
- 운영/관리자 권한은 최소화
- 민감 조회는 승인 및 기록 기반으로 처리

### 7.3 감사와 책임성
- 누가 언제 어떤 민감 데이터에 접근했는지 기록
- 로그/모니터링에 민감 원문이 노출되지 않도록 관리

---

## 8. 대시보드 구성 방향

대시보드는 상담 원문을 직접 읽어 보여주는 방식보다,  
분석 결과를 집계해 보여주는 방식이 적합하다.

포함 항목 예시:

- 감정 변화 추이(주/월/년)
- 감정 분포 비율
- 고민 주제 도넛 그래프
- 기간별 주요 변화 포인트

이렇게 하면 성능과 프라이버시를 함께 지키기 좋다.

---

## 9. 상담 기능과 분석 기능의 역할 분담

- 상담 기능: 대화 경험, 문맥 유지, 사용자 반응 수집
- 분석 기능: 감정/주제 판단, 추세 파악, 대시보드 제공
- 요약 기능: 사용자/봇/요약내용 기반 장기 문맥 유지
- 이벤트 기능: 연간 요약, 모닥불 태우기 경험, 회고 제공

기능을 분리하면 서비스 확장 시 충돌이 줄고 정책 적용이 쉬워진다.

---

## 10. 상담 종료 후 평가 테이블 (신규)

상담 중 메시지 단위 평가(좋아요/싫어요)와 별개로,  
상담이 끝났을 때 사용자가 "해당 상담 전체"를 평가할 수 있도록 별도 테이블을 둔다.

이 항목은 `3.1 채팅 영역`과 `3.5 상담평가 영역`에 함께 반영되어 있으며,  
메시지 단위 평가와 세션 단위 평가를 분리해 관리하는 것을 전제로 한다.

### 10.1 목적
- 세션 단위 만족도 수집
- 품질 개선 지표 확보
- 월간/연간 리포트와 연결

### 10.2 필수 컬럼
- 상담 식별값(어떤 상담을 평가했는지)
- 사용자 식별값
- 평가 시각

평가 데이터는 아래 2개 테이블로 분리한다.

- 상담 평가 점수 테이블: 상담 식별값, 사용자 식별값, 점수, 평가 시각
- 상담 평가 사유 테이블: 상담 식별값, 사용자 식별값, 사유(코드/텍스트), 평가 시각

### 10.3 운영 원칙
- 상담 1건당 사용자 1회 평가를 기본으로 두고, 수정 허용 여부는 정책으로 결정
- 사유는 민감할 수 있으므로 보호 저장 원칙 적용
- 대시보드에는 원문 사유보다 집계 결과 중심으로 노출

---

## 11. 상담 요약 테이블 (사용자T, 봇T, 요약내용T)

메시지 원문과 별도로 상담 요약을 유지하는 전용 테이블을 둔다.

### 11.1 목적
- 원문 데이터 삭제 후에도 상담 맥락 유지
- 봇이 이전 정서/상황을 반영해 친밀도 있는 대화 지속
- 이벤트/쪽지/리포트의 기준 데이터로 활용

### 11.2 핵심 구성
- 사용자 식별값
- 봇 식별값
- 요약내용
- 생성/갱신 시각

### 11.3 활용 원칙
- 봇 응답 생성 시 메시지 원문보다 요약 테이블을 우선 참조
- 메시지 테이블 데이터 삭제 후에도 요약 데이터는 정책 기간 동안 유지
- 요약내용은 민감정보 최소화 원칙으로 작성

### 11.4 사용자/봇 변경 시 DB 변화 규칙
- 상담 요약은 사용자와 봇의 1:1 조합 단위로 관리한다.
- `사용자id`가 같고 `봇id`가 다를 때, 해당 조합의 저장 데이터가 없으면 새로운 데이터를 추가한다.
- `봇id`가 같고 `사용자id`가 다를 때, 해당 조합의 저장 데이터가 없으면 새로운 데이터를 추가한다.
- `사용자id`와 `봇id` 조합이 모두 같으면 새로운 행을 만들지 않고 기존 요약 데이터를 갱신한다.

---

## 12. 최종 결론

현재 기획 방향은 충분히 타당하다.  
특히 아래 세 가지를 중심축으로 유지하면 안정적인 서비스 운영이 가능하다.

- 상담 원문 보호(강한 보안)
- 분석/통계 데이터 분리(대시보드 중심)
- 요약 기반 문맥 유지 + 연간 모닥불 이벤트 중심 삭제 정책

추가로, 상담 평가를 점수/사유 테이블로 분리하고  
상담 요약 테이블을 중심으로 봇 문맥을 유지하면, 원문 삭제 이후에도 사용자 경험을 안정적으로 이어갈 수 있다.

즉, "채팅 경험"과 "데이터 생명주기 관리"를 함께 설계하는 것이 이 프로젝트의 핵심이다.

