# 채팅방 저장 전략 정리 (카카오톡형 UI 기준)

이 문서는 **채팅방 기능만 따로** 정리한 기술 검토 문서다.  
목표는 `카카오톡처럼 대화가 자연스럽게 보이는 채팅방`을 안정적으로 운영하는 것이다.

---

## 1. 요구사항 재정리

- 화면은 카카오톡처럼 채팅 목록/채팅방/메시지 페이징이 자연스럽게 동작해야 함.
- 대화 원문은 장기 보관/조회 가능해야 함.
- 감정 분석/요약/핵심 장면 추출 같은 AI 기능도 필요함.
- DB 담당 관점에서 저장 구조는 단순하고, 운영/복구/감사 가능해야 함.

---

## 2. 제안한 방식 평가  
`로컬DB에 원문 저장 + ChromaDB로 핵심 장면 추출 + 서버DB 저장`

### 2.1 장점

- 로컬에서 빠르게 임시 저장 가능(네트워크 불안정 시 유리).
- 벡터 검색(유사 장면/핵심 문장)에는 ChromaDB가 강함.
- 서버DB에는 요약/핵심 정보만 저장하면 저장량을 줄일 수 있음.

### 2.2 리스크 (중요)

- **원문을 로컬 중심으로 두면 멀티기기 동기화가 어려움** (PC 바뀌면 이력 불완전).
- 서버에서 감사/운영/분쟁 대응 시 원문 근거 부족 가능.
- ChromaDB는 **검색 엔진** 성격이지, 원문 트랜잭션 저장소 대체가 아님.
- 로컬 + 서버 + 벡터DB 3중 구조는 초기 운영 난이도가 높음.

### 2.3 결론

- 이 방식은 **“보조 저장/검색”으로는 좋지만, 메인 저장 전략으로는 비추천**.
- 특히 카카오톡형 채팅 UX처럼 “항상 같은 이력이 보여야 하는 서비스”에는 서버 원본 저장이 더 안전함.

---

## 3. 추천 아키텍처 (우선순위)

## 3-1) 권장안 A (가장 현실적)

**서버 RDB를 원본(Source of Truth)으로 유지 + ChromaDB는 보조 인덱스**

- 원문: 서버 `MESSAGE` 테이블에 1메시지 1행 저장.
- 로컬DB: 미전송 초안/짧은 캐시만 저장.
- ChromaDB: 메시지 임베딩, 핵심 장면 검색, 요약 보조용.
- 분석 결과: `MESSAGE_ANALYSIS`, `COUNSEL_SUMMARY` 등 서버DB에 저장.

**왜 좋은가**
- 멀티기기/백업/권한관리/감사 대응이 안정적.
- 카톡형 메시지 조회(페이징, 검색, 삭제정책) 구현이 쉬움.
- AI 검색 성능은 ChromaDB로 보완 가능.

## 3-2) 대안 B (비용 절감형)

**서버 RDB 원본 + 벡터DB 없이 배치 키워드 인덱스**

- 처음엔 ChromaDB 없이도 운영 가능.
- 핵심 단어/토픽을 배치로 추출해 RDB에 저장 (`message_keyword`, `summary_tag` 등).
- 트래픽/검색 요구가 커지면 ChromaDB를 2차 도입.

**언제 적합한가**
- 초기 MVP, 인프라 단순화가 최우선일 때.

## 3-3) 대안 C (고급 검색 중심)

**서버 RDB 원본 + 검색엔진(OpenSearch/Elasticsearch) + (선택) ChromaDB**

- 키워드 검색은 검색엔진, 의미 검색은 ChromaDB 분리.
- 운영비/복잡도는 가장 높음.

**언제 적합한가**
- 검색 기능이 제품 핵심이고, 대규모 트래픽을 전제로 할 때.

---

## 4. DB 담당 기준 최종 권고

1. **원문 저장은 반드시 서버DB가 기준**으로 가져간다.  
2. **로컬DB는 캐시/초안 용도**로만 제한한다.  
3. **ChromaDB는 검색·요약 보조 인덱스**로만 사용한다.  
4. 원문 삭제 정책(소프트 삭제/연말 퍼지)과 요약 보존 정책을 분리한다.  
5. 초기에는 권장안 A로 시작하고, 필요 시 B/C로 확장한다.

---

## 5. 구현 체크리스트 (채팅방 전용)

- [ ] `MESSAGE` 1행 저장 + `chat_room_id, created_at` 인덱스
- [ ] 채팅방 입장 API: 커서 페이징 (`before/after message_id`)
- [ ] 로컬 캐시 TTL/용량 정책 정의
- [ ] 임베딩 생성 시점 정의 (실시간/배치)
- [ ] ChromaDB 장애 시 fallback (RDB 조회만으로 서비스 가능)
- [ ] 원문 보존기간 vs 요약/임베딩 보존기간 분리

---

## 6. 한 줄 결론

카카오톡형 채팅을 안정적으로 운영하려면 **원문은 서버DB**, **ChromaDB는 보조 검색용**이 가장 안전하고 확장성도 좋다.

---

## 7. 장기 상담 + 멀티PC를 위한 최소 아키텍처 (추가)

질문: **서버DB에 원문을 안 저장해도 다른 PC에서 카톡처럼 볼 수 있나?**  
답: **공용 저장소 없이 사실상 불가능**하다. 다만 평문 대신 암호문을 저장하는 절충안은 가능하다.

### 7.1 권장 절충안: 서버에는 암호문만 저장

- `MESSAGE`에는 평문 대신 `encrypted_payload`, `key_version`, `nonce`, `created_at` 저장.
- 사용자 단말(PC)에서 암복호화 수행.
- 서버는 동기화/백업/순서 보장만 담당하고 내용은 직접 해석하지 않음.

### 7.2 동작 흐름

1. 사용자 A PC에서 메시지 작성 후 로컬에서 암호화  
2. 서버에 암호문 업로드 (`chat_room_id`, `message_seq`)  
3. 사용자 B PC 로그인 시 서버에서 암호문을 내려받아 복호화 표시  
4. 검색/요약은 아래 중 하나 선택
   - (a) 단말에서 요약 후 서버에 요약만 업로드
   - (b) 동의 기반으로 서버 측 분석 파이프라인 사용

### 7.3 장점/주의점

- 장점: 멀티PC 동기화 + 개인정보 보호 강화 가능.
- 주의:
  - 키 분실 시 복구 어려움.
  - 관리자 CS 대응(내용 확인)이 제한됨.
  - 감정 분석 정확도/운영 프로세스가 복잡해짐.

---

## 8. 카카오톡은 보통 어떻게 관리하나 (참고)

공개적으로 알려진 일반 메신저 아키텍처 기준으로 보면:

1. **서버 중심 저장 + 단말 캐시**
   - 메시지 동기화 원본은 서버.
   - 단말은 성능을 위해 로컬 캐시를 둠.

2. **멀티기기 동기화**
   - 새 기기 로그인 시 서버 이력 동기화.
   - 읽음/안읽음, 마지막 메시지 포인터 같은 상태도 서버 기준.

3. **대화 조회 최적화**
   - 채팅방 ID + 시간/시퀀스 인덱스
   - 커서 페이징으로 긴 대화도 빠르게 로딩.

4. **운영 정책 분리**
   - 메시지 보관 정책, 신고/제재, 감사 로그는 별도 운영 체계로 관리.

5. **보안**
   - 서비스 정책에 따라 전송/저장 암호화 적용.
   - 일부 메신저는 기능별로 보안 수준(일반 대화 vs 고보안 대화)을 분리 운영.

즉, “카카오톡처럼 보이게”의 핵심은 UI보다도 **서버 기준 동기화 + 로컬 캐시 + 페이징 인덱스** 조합이다.
